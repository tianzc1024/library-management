server:
  port: 8080

spring:
  profiles:
    active: dev

jwt:
  expiration: 86400000

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto
